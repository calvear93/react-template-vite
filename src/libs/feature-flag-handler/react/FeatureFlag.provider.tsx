import { createContext } from 'react';
import type { FeatureFlagHandler } from '../feature-flag.handler.ts';

// const ThemeContext = createContext<FeatureFlagHandler>(null);

export const FeatureFlagProvider = () => {};
